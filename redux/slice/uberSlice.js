import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const uberSlice = createSlice({
  name: "uber",
  initialState,
  reducers: {},
});

export const {} = uberSlice.actions;

export default uberSlice.reducer;
